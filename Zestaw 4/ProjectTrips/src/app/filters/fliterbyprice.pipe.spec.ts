import { FliterbypricePipe } from './fliterbyprice.pipe';

describe('FliterbypricePipe', () => {
  it('create an instance', () => {
    const pipe = new FliterbypricePipe();
    expect(pipe).toBeTruthy();
  });
});
